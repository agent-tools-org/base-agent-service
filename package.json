{
  "name": "base-agent-service-registry",
  "version": "1.0.0",
  "description": "Agent Service Registry on Base — discover and invoke agent services on-chain",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "deploy": "npx tsx src/deploy.ts",
    "agent": "npx tsx src/agent/service-agent.ts",
    "client": "npx tsx src/client.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  },
  "license": "MIT"
}
